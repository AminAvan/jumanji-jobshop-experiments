environment:
  name: "JobShop-v0"
  num_jobs: 5
  num_machines: 4
  max_num_ops: 4
  max_op_duration: 4

experiment:
  num_episodes: 100
  num_scenarios: 5

random_policy:
  seed: 42

visualization:
  render_episodes: true
  save_plots: true
  plot_dir: "../results/plots"